{% extends 'base.html.twig' %}

{% block title %}Unavailabilities{% endblock %}

{% block body %}
    <h1>Unavailabilities</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unavailability.id }}</td>
            </tr>
            <tr>
                <th>Dates</th>
                <td>{{ unavailability.dates ? unavailability.dates|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('unavailabilities_index') }}">back to list</a>

    <a href="{{ path('unavailabilities_edit', {'id': unavailability.id}) }}">edit</a>

    {{ include('unavailabilities/_delete_form.html.twig') }}
{% endblock %}
