{% extends 'base.html.twig' %}

{% block title %}Unavaibility{% endblock %}

{% block main %}
    <h1>Unavaibility</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Chambre</th>
                <td>{{ unavaibility.room }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ unavaibility.dateDebut ? unavaibility.dateDebut|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ unavaibility.dateFin ? unavaibility.dateFin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('unavaibility_index') }}">back to list</a>

    <a href="{{ path('unavaibility_edit', {'id': unavaibility.id}) }}">edit</a>

    {{ include('unavaibility/_delete_form.html.twig') }}
{% endblock %}
